import { Component, OnInit } from '@angular/core';
import { Router } from '@angular/router';
@Component({
  selector: 'app-insert-login',
  templateUrl: './insert-login.page.html',
  styleUrls: ['./insert-login.page.scss'],
})

export class InsertLoginPage implements OnInit {

  constructor(private router: Router) { }

  ngOnInit() {
  }

}
